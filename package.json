{
  "name": "agency-agents-space",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-site.mjs",
    "build": "npm run generate && node scripts/validate-site.mjs",
    "test": "npm run build",
    "serve": "node scripts/local-server.mjs",
    "browser:smoke": "node scripts/browser-smoke.mjs",
    "report": "node scripts/generate-build-report.mjs",
    "dev": "npx wrangler dev --local --port 8797",
    "cloudflare:deploy": "npm run build && npx wrangler deploy --keep-vars"
  },
  "devDependencies": {}
}
